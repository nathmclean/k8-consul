apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: consul-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: consul
    spec:
      containers:
      - name: consul
        image: consul:v0.7.0
        ports:
        - containerPort: 8300
